export class DurationUtils {
    static readonly ONE_MINUTE = 60;
    static readonly ONE_SECOND = 1;
    static readonly THIRTY_SECONDS = 30;
    static readonly THIRTY_MINUTES = 60 * 30;
    static readonly ONE_HOUR = 60 * 60;
    static readonly ONE_DAY = 60 * 60 * 24;
    static readonly ONE_WEEK = 60 * 60 * 24 * 7;
    static readonly ONE_MONTH = 60 * 60 * 24 * 30;
    static readonly ONE_YEAR = 60 * 60 * 24 * 365;
}