/** Max upload size for portal profile avatar (bytes). */
export const PROFILE_AVATAR_MAX_BYTES = 5 * 1024 * 1024

/** Max upload size for director signature image (bytes). */
export const PROFILE_SIGNATURE_MAX_BYTES = 2 * 1024 * 1024

export const PROFILE_IMAGE_ACCEPT = 'image/jpeg,image/png,image/webp'
